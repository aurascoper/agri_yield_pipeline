__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/
.ipynb_checkpoints/
.DS_Store
data/yields/*.csv
data/yields/*.parquet
ee-service-account*.json
# Ship fields.yml + series parquets so the Render demo has live Fields data.
# GeoTIFFs stay local — too big + too tenant-specific to commit.
data/fields/**/*.tif
.claude/
